package main

var (
	// Config paths used by more than one test
	invalidPath     = "/tmp/some/file-that-does-not-exist"
	invalidRepoPath = "../tests/data/config/test-invalid-repo.yml"
	smallRepoPath   = "../tests/data/config/small.yml"
)
